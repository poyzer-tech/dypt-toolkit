{
  "name": "dypt-toolkit",
  "version": "0.1.0",
  "private": true,
  "description": "Public dypt toolkit resources for AI agent and CLI workflows.",
  "scripts": {
    "release:check": "git diff --check && test -f skills/dypt-cli/SKILL.md && test -f docs/install-codex.md && test -f docs/install-claude.md",
    "release:tag": "git tag v$npm_package_version"
  }
}
